# Picssr samples

Sample photos with geolocation data in the file name for use in environments where there is no photo library (tests, desktop, etc). The usable samples are in the `samples` directory.

To add new samples, first drop the images in the `original` directory and run the `formatter.sh` script. Only `.heic` format and running the script on MacOS is supported.
